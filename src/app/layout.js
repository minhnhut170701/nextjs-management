import ToolBar from "./components/ToolBar";
import "./globals.css";
import { Inter } from "next/font/google";
import ProviderCustom from "./components/provider";

const inter = Inter({ subsets: ["latin"] });

export const metadata = {
  title: "Ecommerce Management",
  description: "Generated by DNTU Student",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <div className="flex space-x-10 overflow-hidden">
          <div className="h-screen w-[300px] bg-black p-10 relative z-40">
            <ToolBar />
          </div>
          <ProviderCustom>{children}</ProviderCustom>
        </div>
      </body>
    </html>
  );
}
